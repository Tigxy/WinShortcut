<UserControl x:Class="win_short_cut.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:win_short_cut"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      >
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="UserControl">
            <Setter Property="Background" Value="White"/>
        </Style>
    </d:DesignerProperties.DesignStyle>

    <StackPanel VerticalAlignment="Center">
        <!--<TextBlock Text="Current path for shortcuts"/>
        <TextBlock x:Name="tb_current_path" Text="{Binding Path=ShortcutPath, TargetNullValue=dir/home}"/>
        <TextBox x:Name="tb_new_path"/>-->

    </StackPanel>
</UserControl>
